# Calcule a distância entre dois pontos utilizando a formula da distância = raiz((x2 − x1)^2 + (y2 − y1)^2).
import math

x1, y1 = float(input("Introduza as coordenadas do primeiro ponto: x1=")), float(input("y1="))
x2, y2 = float(input("Introduza as coordenadas do segundo ponto: x2=")), float(input("y2="))
distancia = math.sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1))
print("A distancia é: ", distancia)
